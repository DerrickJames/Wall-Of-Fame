export const ROOT = './';
export const ROOT_SRC = `${ROOT}src/`;
export const APP_SRC = `${ROOT_SRC}app/`;
export const TEMP = `${ROOT}.tmp/`;

export const CONFIG = {
    less: `${ROOT_SRC}styles/styles.less`,
    tempCss: `${TEMP}styles/`
};
